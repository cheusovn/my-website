---
meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  scriptName: null
  command: apt-get install -y fonts-dejavu-core 2>/dev/null || true && python bot.py
  persistenceMount: /data
  containerPort: 80
  servicePort: 80
